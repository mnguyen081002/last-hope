# Last Hope — Code Map

Bảng tra cứu nhanh: hệ thống nào đã có, ở file nào, API chính là gì, test tới đâu. Đọc file này (+ `BACKLOG.md`) đầu mỗi session thay vì quét lại `Assets/`. Cập nhật cùng commit mỗi khi thêm/đổi hệ thống — không để lệch code thực tế.

Quy ước cột "Test": ⬜ chưa có test · 🟡 có test một phần · ✅ có EditMode/PlayMode test bao phủ chính.

---

## Assembly map (dependency một chiều)

```
LastHope.Data ← LastHope.Core ← LastHope.Systems ← LastHope.Presentation / LastHope.UI / LastHope.DebugTools
Tests.EditMode / Tests.PlayMode → tham chiếu tất cả assembly trên
LastHope.EditorTools (Editor-only) → Core, Data, Systems, Presentation, DebugTools, UI,
                                      Unity.InputSystem, Unity.TextMeshPro, UnityEngine.UI, URP Runtime
```

Presentation và UI (từ S5) đều thêm reference `Unity.InputSystem`, `Unity.TextMeshPro`, `UnityEngine.UI` (cần cho Interaction/InventoryPanel).

## Scene flow

`00_Boot` (BootLoader) → additive `10_GamePersistent` (services + Player/Camera/HUD Canvas sống suốt phiên) → **`SceneFlowController` (S6) load scene gameplay đầu tiên** theo `LocationDefinition.SceneName` của `Player.CurrentLocationId` (mặc định `location_shelter` → `20_MainShelter`), và load/unload lại mỗi khi `TravelCompleted`/`WorldStateReloaded` bắn ra. `90_TestSystems` (Ground/ScaleRef/Light) không còn nằm trong luồng boot tự động — chỉ để mở tay trong Editor kiểm tra scale.

---

## LastHope.Core

| File | Class | API chính | Test |
| --- | --- | --- | --- |
| `Assets/Game/Core/Logging/GameLog.cs` | `GameLog` (static) + `LogCategory` enum | `Info/Warn/Error(LogCategory, string)` | ⬜ |
| `Assets/Game/Core/State/WorldState.cs` | `WorldState` + `RouteState` (S8: real fields — `FloodLevel`/`CurrentLevel`/`Contamination`/`Closed`/`Electrical`(inert)/`Modifiers`(inert, reserved P3+); chỉ là cache hiển thị, HazardSystem ghi mỗi long-tick, BeginTravelCommand KHÔNG đọc field này mà tự tính fresh) + **`NpcState`** (S15, viết lại thật — bỏ `Id`/`StatusName` cũ: `Id`/`LocationId`/`Health:NpcHealthState{Healthy,Injured,Critical,Dead}`/`Hunger`/`Thirst`/`Trust`(0-100)/`Recruited`/`CurrentTaskId`/`Flags:HashSet`/`Inventory` (owner `npc:<id>`, lazy qua `InventoryOwnerResolver` — chỉ tạo field Inventory, KHÔNG tự tạo NpcState, việc đó là của `RecruitNpcCommand` S16); S15 chỉ dựng shape, chưa có system nào mô phỏng tiêu thụ/trust (S16) + **`IntelState`/`IntelRecord`/`IntelConfidence`** (S15) — `WorldState.Intel:IntelState{Records:Dict<subjectId,IntelRecord>}`; `IntelRecord{SubjectId,Kind("route"/"location"),Confidence,ObservedAtMinute,FloodLevel?,CurrentLevel?,Closed?}` — payload phẳng (route field null cho subject không phải route), không dùng Dict tự do vì chỉ 1 loại subject cần payload tới giờ; `IntelConfidence` enum `Unverified<Uncertain<Reliable<Confirmed` + **`ActiveEventState`** (S13, viết lại thật — bỏ `Id`/`StatusName` cũ: `EventInstanceId`/`EventId`/`State:EventLifecycleState`/`TriggeredAtMinute`/`DeadlineMinute`/`ChosenResponse`; S14 thêm `SoftDeadlineMinute`/`SoftDeadlineNotified` — deadline arm lúc instance thành Active (trigger, hoặc discovery với event RequiresDiscovery); `EventLifecycleState` enum đủ 8 trạng thái event-system-design.md §5, S14 đi qua Undiscovered/Active/Resolved/Expired/PersistentConsequence) + `LocationState` (S6) + **`ShelterState`** (S6; S10 thêm StructuralIntegrity/WaterIntrusion/LivingCapacity/Occupants/BuildSlots/WaterStocks/EventFlags; S11 thêm `Modules:Dict<instanceId,ModuleState{InstanceId,ModuleId,SlotId,Durability,Active}>`; S12 thêm `Power:ShelterPowerState{BatteryCharge,Priorities:Dict<moduleInstanceId,PowerPriority>}` — cuối cùng cũng đủ hết field plan gốc S10 liệt kê) + **`ShelterEventFlags`** (S13, static class hằng string cho `ShelterState.EventFlags` — đặt ở Core.State chứ KHÔNG phải trong `WaterIntrusionSystem` (Systems) vì `ResolveEventCommand` (Core.Commands) cũng cần đọc/ghi cùng tên cờ và Core không được phụ thuộc Systems — phát hiện lúc build ra circular assembly reference, sửa bằng cách chuyển hằng số lên Core) + `ActiveTaskState` (S11, viết lại thật — bỏ `Id`/`StatusName` cũ: `TaskId`, `Kind:TaskKind{Active,Passive}`, `TargetId`, `ModuleId` (chỉ Build task set), `Progress`, `Status:TaskStatus{Running,Paused}`, `RequiredWorker` — Passive luôn chạy kể cả FastForward, Active cần RequiredWorker ở shelter, chưa ai tạo Active task tới S13) + `WorldState.TaskInventories:Dict<taskId,InventoryState>` (S11, owner `task:<id>`, nguồn sự thật duy nhất cho vật liệu đã reserve — không có list `ReservedItemInstanceIds` riêng, tránh double-bookkeeping) | Root state graph: `WorldTimeMinutes`, `CurrentDisasterPhase`, `RandomSeed`, `RngStreams`, `Player`, `PlaythroughId` (S6), các Dictionary state theo id | 🟡 (gián tiếp qua SearchPointTests/OwnerResolverTests/TravelTests/WaterIntrusionSystemTests/BuildCommandsTests/EventCommandsTests) |
| `Assets/Game/Core/State/SearchPointState.cs` (S6) | `SearchPointState` | `{SearchPointId, Rolled, Inventory:InventoryState}` — roll 1 lần, Inventory là InventoryState đầy đủ nên TransferItemCommand dùng lại nguyên vẹn | ✅ (SearchPointTests) |
| `Assets/Game/Core/State/PlayerState.cs` | `PlayerState` | `ActorId`, `CurrentLocationId`, `Inventory`, **`Condition:PlayerConditionState`** (S7) | ⬜ |
| `Assets/Game/Core/State/PlayerConditionState.cs` (S7) | `PlayerConditionState` + `StatusEffectState` + `IncapacitationState` enum | `Health/Stamina/Fatigue/Hunger/Thirst` 0-100 (Hunger/Thirst: 0=không cần, 100=cạn — ngược chiều Health), `BodyTemperatureC`, `StatusEffects:Dict<id,{Severity,AppliedAtMinute}>`, `Exposures:Dict<hazardId,float>` (cộng dồn, không clamp 0-100), `Incapacitation`, **`TreatingExposure:bool`** (S9, cờ tạm trong phiên `RestAtShelterCommand.TreatExposure`) | ✅ (ConditionOpsTests/ConditionSystemTests/RestAtShelterCommandTests) |
| `Assets/Game/Core/State/InventoryState.cs` | `InventoryState` + `OverloadState` enum | `Items` (instanceId→ItemInstanceState), `CurrentWeightKg/VolumeLiters`, `Overload` (nay được `Systems.Inventory.InventorySystem` set, xem dưới) | ✅ (InventoryRulesTests) |
| `Assets/Game/Core/Rules/ConditionOps.cs` (S7) | `ConditionOps` (static) | Mutator clamp thuần trên `PlayerConditionState`: `ApplyHealth(floor tuỳ chọn)/ApplyStamina/ApplyFatigue/ApplyHunger/ApplyThirst`, `SetStatusSeverity/GetStatusSeverity` (severity≤0 → xoá khỏi dict), `AddExposure/GetExposure` (cộng dồn, floor 0), `ApplyExposureStatusChain` (threshold `BlackWaterExposureThreshold`/`SickExposureThreshold` → status `black_water_exposure`/`sick`, tự xoá khi hồi phục), `RecomputeIncapacitation`/`IsIncapacitated` (Health≤`CollapsedHealthThreshold` → Collapsed). Status id const: `StatusWet/Cold/Bleeding/Sick/BlackWaterExposure/Disoriented` | ✅ (11 test) |
| `Assets/Game/Core/Rules/HazardRules.cs` (S8) | `HazardRules` (static) | `EvaluateRoute(route, phasesByStart, atMinute) → {FloodLevel,CurrentLevel}` — tìm phase hiện tại + phase kế tiếp trong `DisasterPhasesSorted`, lerp band theo tiến độ qua phase kế, trừ `BaseElevationLevel`, clamp 0-`MaxLevel`(4). `ComputeLevel` là hàm thuần tách riêng — DÙNG CHUNG cho HazardSystem (live) và ReturnWindowCalculator (forecast), không drift | ✅ (7 test) |
| `Assets/Game/Core/Rules/TravelRules.cs` (S8) | `TravelRules.EvaluateCrossing` + `CrossingEvaluation` | Tier = max(FloodLevel, CurrentLevel sau khi trừ `EquipmentProtection.CurrentReduction`). Tier≥4 → Impassable (chặn hẳn). Tier 0-3: tra `HazardBalance` array (stamina/exposure/wet/time theo tier), jacket nhân `WetMultiplier` vào wet, boots chặn/giảm exposure theo `BootsBlockLevel`. Thiếu stamina → chỉ warning, KHÔNG chặn (chuyển Fatigue ở BeginTravelCommand) | ✅ (9 test) |
| `Assets/Game/Core/Rules/ReturnWindowCalculator.cs` (S8) | `ReturnWindowCalculator.Evaluate` + `ReturnWindow{MinutesUntilWorse,MinutesUntilImpassable}` | Sample `HazardRules.EvaluateRoute` mỗi 10 phút tới horizon 24h (deterministic, không RNG) — cùng công thức HazardSystem dùng live nên forecast World Map không bao giờ nói sai | ✅ (3 test) |
| `Assets/Game/Core/Rules/EquipmentRules.cs` (S8) | `EquipmentRules` + `EquipmentProtection` (struct) | `SumProtection`/`HasProtection` đọc `ItemDefinition.Protection` của item đang trong `InventoryState.EquipmentSlots`; `ResolveTravelProtection` gộp rope(`current_reduction`)/jacket(`wet_multiplier`)/boots(`exposure_block_level`+`exposure_medium_multiplier`) thành 1 struct cho TravelRules | ✅ (4 test) |
| `Assets/Game/Core/Rules/WaterIntrusionRules.cs` (S10; +Barrier S11) | `WaterIntrusionRules` (static) | `ComputeDelta(rainIntensity, backflowActive, activePumpCount, hasActiveBarrier, cfg)` — units_delta thuần = inflow(bảng theo `RainIntensity`, ×30% nếu có Barrier active, +backflow — backflow KHÔNG bị Barrier chặn) − passive_drain(0 khi backflow) − pump_output; `LevelFor(units, cfg)` → `WaterIntrusionLevel` theo threshold Damp/Shallow/Deep/Critical; `Clamp01To100`. Dùng chung cho `WaterIntrusionSystem` (live) — sẵn cho forecast UI sau này, giống `HazardRules` | ✅ (WaterIntrusionRulesTests, 9 test) |
| `Assets/Game/Core/Rules/BuildRules.cs` (S11) | `BuildRules` (static) + `PlacementIssue` enum | `TryFindZoneForSlot(zones, slotId, out zone)` — scan tuyến tính `ShelterZoneDefinition.BuildSlotIds`; `ValidatePlacement(zones, shelter, slotId, module)` → `PlacementIssue` (InvalidSlot/SlotLocked/SlotOccupied/WrongZone/None) — KHÔNG nhận GameContext, trả enum thuần để command tự dịch sang CommandErrorCode (giống TravelRules.EvaluateCrossing); `HasMaterials(inv, materials)` cộng dồn nhiều stack cùng item id; `DismantleRefund(materials)` 50% làm tròn xuống | ✅ (BuildRulesTests, 11 test) |
| `Assets/Game/Core/Rules/PowerRules.cs` (S12) | `PowerRules` (static) + `PowerDemandEntry`/`PowerAllocationResult` (struct) | `Allocate(gridAvailable, batteryCharge, demands, cfg)` — thuần, 1 long-tick (10'): sort Critical→Disabled, Grid trước rồi Battery (battery đo unit-phút, quy đổi qua `/10` để so với demand tức thời), Disabled không bao giờ nhận điện; Grid dư → sạc battery `BatteryChargeRatePerLongTick`; trả `Powered:Dict<moduleInstanceId,bool>` + `NewBatteryCharge` | ✅ (PowerRulesTests, 8 test) |
| `Assets/Game/Core/Rules/EventTriggerRules.cs` (S13; +S14) | `EventTriggerRules` (static) | `Evaluate(def, currentPhase, shelter, hasActivePump, roll)` — AND phẳng mọi điều kiện `EventDefinition` có đặt (Phase id/BlackWater, State min level, Pump active, Chance roll — `roll` được EventSystem tự roll qua stream "events" rồi truyền vào, giữ hàm này thuần/test được không đụng RNG); S14: `ShouldWakeSleeper(priority, atShelter)` — Critical luôn wake, Major wake khi tại shelter, còn lại không | ✅ (EventTriggerRulesTests 12 + qua EventLifecycleTests) |
| `Assets/Game/Core/Rules/IntelRules.cs` (S15) | `IntelRules` (static) | `EffectiveConfidence(record, nowMinute, cfg)` — suy giảm theo tuổi thông tin: 1 bậc sau `ConfirmedToReliableMinutes` (60'), 2 bậc sau `ReliableToUncertainMinutes` (180'), floor tại `Unverified`, áp dụng từ confidence lúc quan sát (Reliable cũng suy giảm tiếp, không chỉ Confirmed); `ShouldReplace(existing, incoming)` — record mới hơn (hoặc cùng phút) luôn thắng, bất kể confidence, kể cả `existing=null` | ✅ (IntelRulesTests, 9 test) |
| `Assets/Game/Core/Commands/RestAtShelterCommand.cs` (S9) | `RestAtShelterCommand`, `RestMode` enum | Chỉ dùng được khi `LocationDefinition.IsShelter`. `Rest`: FastForward `ShelterRestMinutes`. `TreatExposure`: cần item Tags chứa "medical" (fail `NoMedicalItem` nếu không) → tiêu thụ 1, áp `ConditionOps.ApplyItemUseEffects` (vd medkit vẫn heal như dùng thường), bật `PlayerConditionState.TreatingExposure` rồi FastForward `ShelterTreatExposureMinutes` — trong lúc đó `ConditionSystem.OnLongTick` tự trừ thêm exposure `ShelterTreatExposureDecayPerLongTick`/long-tick, tắt cờ khi xong. `DryOff`: tức thì, không tốn giờ, xoá status Wet về 0 | ✅ (RestAtShelterCommandTests, 5 test) |
| `Assets/Game/Core/Rules/InventoryRules.cs` | `InventoryRules` (static) | **`EffectiveCapacity(inv,defs,balance)`** (S8, `defs` optional — dry bag equipped ở slot "back" override 10kg/18L thay 15kg/25L mặc định), `ComputeOverload(inv,balance,defs=null)` (dùng EffectiveCapacity), `CanAccept(dest,defs,balance,itemId,qty)` (chặn ở 150% hard cap dùng EffectiveCapacity, container không giới hạn luôn true), `SpeedModifierFor`, `LoadFactorFor` (S6), `IsCapacityLimited(ownerId)` (chỉ "player") | ✅ (8 test cũ + 1 dry-bag) |
| `Assets/Game/Core/State/ItemInstanceState.cs` | `ItemInstanceState` + `ContaminationState`/`WetState` enum | `InstanceId`, `ItemId`, `Quantity`, `Condition`, `Durability`, `Contamination`, `Wet`, `ContainerId` | ⬜ |
| `Assets/Game/Core/State/InventoryOps.cs` | `InventoryOps` (static) | `RecalculateLoad(inv, defs)` (chỉ tính tổng weight/volume, KHÔNG set Overload); `AddItem(inv, defs, itemId, qty, idGen)` (merge stack theo MaxStackSize, không kiểm capacity) | ⬜ |
| `Assets/Game/Core/Random/RngStream.cs` | `RngStream` + `RngStreamState` | xorshift64* trên state `ulong` mutable, `NextInt(min,maxExcl)`, `NextDouble()` | ✅ |
| `Assets/Game/Core/Random/RngService.cs` | `RngService` | `GetStream(name)` — named stream derive từ `WorldState.RandomSeed ⊕ FNV1a64(name)`, state sống trong `WorldState.RngStreams` | ✅ |
| `Assets/Game/Core/Save/WorldStateSerializer.cs` | `WorldStateSerializer` (static) | `Serialize(WorldState)` (indented), `SerializeCanonical(WorldState)` (Formatting.None, dùng cho checksum/deep-compare), `Deserialize(json)`, `Settings` (snake_case, StringEnumConverter, ObjectCreationHandling.Replace) | ✅ |
| `Assets/Game/Core/Save/SaveFile.cs` | `SaveFile`, `SaveSlotInfo` | `SaveFile{SaveVersion,DefinitionVersion,SavedAtUtc,Checksum,SlotId,World(JRaw)}` — World embed verbatim, không re-serialize | ✅ |
| `Assets/Game/Core/Save/SaveService.cs` | `SaveService`, `SaveResult`, `LoadResult` | `Autosave(world)` (round-robin autosave_0/1/2), `SaveToSlot(world,slotId)` (atomic: tmp→verify→backup cũ→rename), `Load(slotId)`, `ListSlots()`. Checksum SHA256 trên world payload canonical | ✅ |
| `Assets/Game/Core/Events/EventBus.cs` | `EventBus` (+ private `EventChannel<T>`) | `Subscribe<T>/Unsubscribe<T>/Publish<T>` — struct event, copy-on-write handler array, zero-boxing | ✅ (gián tiếp qua CommandPipelineTests) |
| `Assets/Game/Core/Events/GameEvents.cs` | `IGameEvent` + 20 struct: `WorldTimeChanged`, `DisasterPhaseChanged`, `RouteStateChanged`, `ShelterWarningRaised`, `TaskCompleted`, `EventDiscovered`, `InventoryChanged`, `NpcStateChanged`, `OverloadStateChanged`, `WorldStateReloaded`, `ItemTransferred`, `SearchPointOpened`, `ContainerViewRequested`, `TravelStarted`, `TravelCompleted`, `ConditionChanged`, `StatusEffectChanged`, `EquipmentChanged`, `WorldMapRequested`, `ExclusivePanelOpened` (2026-07-24, UI-routing: `{PanelName}` — panel "focused" nào mở thì các panel focused khác tự đóng, tránh chồng chữ), `ShelterWaterChanged` (S10, chỉ khi Level đổi), `TaskStateChanged`/`BuildProgressChanged`/`ModuleCompleted` (S11 — `ModuleCompleted` tái dùng cho cả complete lẫn dismantle, phân biệt bằng `ModuleInstanceId` null hay không), `PowerStateChanged`/`WaterStocksChanged`/`SleepStarted`/`SleepInterrupted`/`SleepEnded` (S12), **`EventTriggered`/`EventResolved`** (S13), **`EventDeadlineApproaching`/`EventExpired`** (S14 — approaching publish 1 lần khi qua soft deadline, mang theo hard deadline cho countdown UI; expired khi qua hard deadline không resolve), **`IntelUpdated`** (S15, publish mỗi khi 1 subject có record intel mới/bị thay), **`NpcRecruited`/`NpcDied`** (S16), **`EvacuationDeclared`/`OutcomeReached{Outcome}`** (S18) | ⬜ |
| `Assets/Game/Core/Time/GameTimeUtil.cs` | `GameTimeUtil` (static) | `DayIndex(m)`, `TimeOfDayMinutes(m)`, `Format(m)` — anchor Day 0 17:00 = phút 0 | ⬜ (gián tiếp qua TickSchedulerTests) |
| `Assets/Game/Core/Time/SimulationClock.cs` | `SimulationClock` | `AccumulateRealSeconds(double)`, `TryConsumeMinute()`, `PendingGameSeconds`. **Bank dùng `decimal` nội bộ** (không phải double) — double cộng dồn ~17k lần bị lệch 1 phút/24h, xem comment trong file | ✅ |
| `Assets/Game/Core/Time/TickScheduler.cs` | `TickScheduler` | `SubscribeShort/Long(Action<long>)`, `RegisterThreshold(minute, cb)`, `Advance(clock, maxMinutes)` (bounded catch-up), `FastForward(minutes)` (Sleep/Travel), **`FastForward(minutes, Func<long,bool> interrupt) → int elapsed`** (S12, overload cộng thêm — check sau MỖI phút, dừng sớm khi interrupt trả true; API cũ không đổi). `AdvanceOneMinute()` private — NƠI DUY NHẤT tăng `WorldTimeMinutes` | ✅ |
| `Assets/Game/Core/Commands/IGameCommand.cs` | `IGameCommand`, `CommandResult`, `CommandErrorCode` (+`NotAtLocation` S6, +`Incapacitated`/`SlotMismatch` S7, +`SlotOccupied`/`SlotLocked`/`MissingMaterials`/`TaskNotFound`/`TaskNotRunning` S11, +`NoPower`/`NoFilter`/`NothingToPurify`/`NoBedAvailable`/`UnsafeToSleep` S12, +`EventNotActive`/`ResponseUnavailable` S13, +`EventNotDiscovered` S14, +`NpcUnavailable`/`NpcNotRecruited`/`CapacityFull` S16 — S18 dùng lại `InvalidState`/`NotAtLocation` sẵn có cho `EvacuateCommand`, không thêm code mới), `GameContext` | `GameContext{World,Definitions,Events,Rng,Clock}` — bundle inject duy nhất | ✅ |
| `Assets/Game/Core/Commands/CommandProcessor.cs` | `CommandProcessor` | `Submit(IGameCommand) → CommandResult` — stamp WorldTime, Validate→Execute, log lỗi qua GameLog | ✅ |
| `Assets/Game/Core/Commands/InventoryOwnerResolver.cs` | `InventoryOwnerResolver` (**public** static, đổi từ internal ở S6 để UI đọc được) | `TryResolve(ctx, ownerId, out inv)` — scheme: `"player"`, `"searchpoint:<id>"` (KHÔNG tự tạo state, fail nếu chưa `Rolled`), `"shelter_storage:<id>"` (lazy-create), `"location_dropped:<id>"` (lazy-create), **`"task:<taskId>"`** (S11, lazy-create trong `WorldState.TaskInventories` — `StartBuildCommand` reserve vật liệu vào đây, `TaskSystem` tiêu thụ lúc complete, `CancelTaskCommand` trả về `shelter_storage:`). **`"npc:<id>"`** (S15, lazy-create field `Inventory` trên `NpcState` ĐÃ TỒN TẠI — khác các owner kind kia, KHÔNG tự tạo cả `NpcState`, việc đó là của `RecruitNpcCommand` S16; fail nếu chưa recruit) | ✅ (OwnerResolverTests, 6 test) |
| `Assets/Game/Core/Commands/UseItemCommand.cs` | `UseItemCommand` | Giảm quantity item trong inventory actor, publish `InventoryChanged`. S7: Validate chặn owner=player+Incapacitated trừ item có Tag "medical" (→`Incapacitated`); Execute áp `ItemDefinition.UseEffects` vào `Player.Condition` qua `ConditionOps`, publish `ConditionChanged` nếu có đổi | ✅ |
| `Assets/Game/Core/Commands/TransferItemCommand.cs` | `TransferItemCommand` | Chuyển item giữa 2 owner đã biết; move nguyên instance nếu chuyển hết quantity (giữ Condition/Contamination/Wet), chỉ split khi chuyển một phần. Validate kiểm `InventoryRules.CanAccept` → fail `InventoryFull`; Execute publish thêm `ItemTransferred`. S8: đích=player + item Contaminated + không gloves (`EquipmentRules.HasProtection "handles_contaminated"`) → +exposure "black_water" (cost-not-block, vẫn cho chuyển) | ✅ |
| `Assets/Game/Core/Commands/EquipItemCommand.cs` (S8) | `EquipItemCommand`, `UnequipItemCommand` | Equip: validate `ItemDefinition.EquipSlot` khớp slot yêu cầu (sai → `SlotMismatch`, không equippable → `InvalidTarget`) → ghi `InventoryState.EquipmentSlots[slot]=instanceId` (item vẫn nằm trong Items, equip chỉ là tham chiếu slot, không di chuyển). Unequip: xoá slot (rỗng sẵn → `SlotMismatch`). Cả 2 publish `EquipmentChanged` | ✅ (6 test) |
| `Assets/Game/Core/Commands/StartSleepCommand.cs` (S12, viết lại) | `StartSleepCommand` | Validate: chặn Incapacitated + shelter ngập Deep+ (`UnsafeToSleep`; `NoBedAvailable` khai báo sẵn nhưng chưa dùng — chưa có Bed module thật). Execute: `ctx.Clock.FastForward(minutes, interrupt)` (overload mới) — thức sớm nếu ngập lên Deep+ giữa lúc ngủ, publish `SleepStarted`/`SleepInterrupted(minutesSlept)`/`SleepEnded`. S14: thêm wake theo Event priority (`EventTriggerRules.ShouldWakeSleeper` — Critical luôn wake, Major wake khi ngủ tại shelter, Standard ngủ tiếp; tính cả event Undiscovered trigger sau lúc bắt đầu ngủ) | ✅ (SleepInterruptTests 7 + EventLifecycleTests 4 sleep test) |
| `Assets/Game/Core/Commands/OpenSearchPointCommand.cs` (S6; hybrid loot 2026-07-24) | `OpenSearchPointCommand` | Validate: đúng location (`NotAtLocation`). Execute: roll MỘT LẦN qua stream "loot" (bỏ qua nếu đã `Rolled`) — entry `Guaranteed` luôn spawn, còn lại theo `loot.NextInt(0,100) < entry.Chance`; `OpenTimeMinutes>0` → FastForward, publish `SearchPointOpened`+`ContainerViewRequested` | ✅ (SearchPointTests, 8/8) |
| `Assets/Game/Core/Commands/TaskCommands.cs` | `StartTaskCommand`/`CancelTaskCommand`/`PauseTaskCommand`/`ResumeTaskCommand` (S11, viết lại thật — trước chỉ validate+flag), `BeginTravelCommand` (S6 đầy đủ; **S8**: Validate thêm chặn Incapacitated + `TravelRules.EvaluateCrossing` không Passable → `RouteBlocked`; Execute: `minutes = ceil(TravelMinutes × loadFactor × crossing.TimeFactor)`, sau FastForward áp cost — stamina (thiếu hụt → Fatigue, không chặn), exposure "black_water", wet — qua `ConditionOps`, publish `ConditionChanged`. Hazard tính FRESH mỗi lần gọi qua `HazardRules`/`EquipmentRules`, KHÔNG đọc `RouteState` cache) | ✅ (TravelTests 9 + BuildCommandsTests 9 cho Task) |
| `Assets/Game/Core/Commands/BuildCommands.cs` (S11) | `StartBuildCommand`, `DismantleModuleCommand` | Start: validate ở shelter + module tồn tại + slot chưa có task khác + `BuildRules.ValidatePlacement` + `HasMaterials` → reserve vật liệu vào `task:<taskId>` (helper `MoveMaterials` gộp nhiều stack cùng item id — khác `TransferItemCommand` vốn chỉ chuyển 1 instance cụ thể) → tạo `ActiveTaskState` Passive, **S18: `DecisionLog.Append(ctx,"build",moduleId)`**. Dismantle: refund 50% qua `BuildRules.DismantleRefund`, xoá `ModuleState`, mở khoá slot, publish `ModuleCompleted(slotId, null)` (tái dùng event, null instance id = slot trống) | ✅ (BuildCommandsTests, 9 test) |
| `Assets/Game/Core/Commands/ShelterCommands.cs` (S12) | `SetPowerPriorityCommand`, `StartPurifyBatchCommand`, `CollectWaterCommand` | SetPowerPriority: ghi `ShelterPowerState.Priorities[moduleInstanceId]`, PowerSystem long-tick sau đó mới quyết định có điện hay không. StartPurifyBatch: **cùng dạng validate-rồi-FastForward như RestAtShelterCommand** (không phải Active task giám sát) — cần module Active(có điện)+Durability>0(còn filter)+đủ Untreated, sau 60' đổi 3 Untreated→3 Clean, trừ Durability (tái dùng field này làm "tuổi thọ filter" — khác nghĩa Barrier dùng nó làm kết cấu, có ghi chú rõ). CollectWater: đổi Clean Water trừu tượng thành `item_water_bottle` cụ thể (item P1 có sẵn) để dùng UseItemCommand uống được | ✅ (ShelterCommandsTests, 10 test) |
| `Assets/Game/Core/Commands/ResolveEventCommand.cs` (S13; +S14; +S16; +S17) | `ResolveEventCommand` | Validate: event instance tồn tại + State==Active (`EventNotActive`; S14: Undiscovered → `EventNotDiscovered` riêng) + response nằm trong `AvailableResponses` (`ResponseUnavailable`). Execute: switch nhỏ theo response id — "reinforce_seal"/"clear_pump" xoá cờ + FastForward(15), "secure_storage" chỉ resolve, "greet"/"help_search"/"turn_away" (S16, chain Minh) + **"rescue"/"leave" (S17, School Rescue)** set `PersistentFlags` tương ứng, "ack" không làm gì; luôn set `State=Resolved` + `ChosenResponse`, **S18: `DecisionLog.Append(ctx,"event:"+eventId,responseId)`**, publish `EventResolved` (→ `EventSystem.OnEventResolved` đọc `ResponseId` này để chain rẽ nhánh) | ✅ (EventCommandsTests 9 + EventLifecycleTests chain-by-response) |

## LastHope.Data

| File | Class | API chính | Test |
| --- | --- | --- | --- |
| `Assets/Game/Data/Definitions/DefinitionBase.cs` | `DefinitionBase` (abstract) | `Id`, `DisplayNameKey`, `DataVersion` | ⬜ |
| `Assets/Game/Data/Definitions/ItemDefinition.cs` | `ItemDefinition` | `Category`, `BaseWeightKg`, `BaseVolumeLiters`, `MaxStackSize`, `MaxDurability`, `WaterResistance`, `Tags`, `TwoHandCarry` (S5), `UseEffects:Dict<string,float>` (S7), `EquipSlot` (S7 placeholder, **dùng thật từ S8** bởi `EquipItemCommand`: "body"/"feet"/"hands"/"back"/"tool"), `Protection:Dict<string,float>` (S7 placeholder, **dùng thật từ S8** — key: `current_reduction` rope, `wet_multiplier` jacket, `exposure_block_level`+`exposure_medium_multiplier` boots, `handles_contaminated` gloves, `backpack_capacity_kg`+`backpack_capacity_liters` dry bag. **Không có `ProtectionSpec.cs` riêng** — dict đã đủ, tránh trùng lặp cấu trúc, xem ghi chú cuối file) | 🟡 (qua ContentValidationTests) |
| `Assets/Game/Data/Definitions/LocationDefinition.cs` | `LocationDefinition` | `SearchPointIds`, `ConnectedRouteIds`, `SceneName` (S6), **`IsShelter`** (S7, data-driven cho ConditionSystem body-temp regen/wet-dry — `location_shelter` là location duy nhất `true`) | 🟡 |
| `Assets/Game/Data/Definitions/DisasterPhaseDefinition.cs` (S7; content thay ở S17) | `DisasterPhaseDefinition` | `StartMinute`(long), `FloodBandMin/Max`, `CurrentBandMin/Max`, `BlackWater`, `RainIntensity`. **S17: `phases_p2.json` (4 phase test) → `phases_p4.json`** (8 phase timeline slice thật theo bảng baseline: Normal@0/Warning@120/FirstRain@300/BlackRain@480/Escalation@640/Peak@760/Aftermath@880/End@960) — xoá hẳn file cũ (không phải dead code ngoài phạm vi, đây chính là nội dung "S17: 4 phase" kế thừa; đã xác nhận trước khi xoá không test nào đọc file JSON thật, chỉ dùng fixture in-memory). `ShelterBalance.InflowByRainIntensity` nối dài `[0,2,4,6]`→`[0,2,4,6,9]` cho rain_intensity=4 ở Peak | 🟡 (qua ContentValidationTests + SliceTimelineTests + DisasterPhaseSystemTests fixture) |
| `Assets/Game/Data/Definitions/RouteDefinition.cs` | `RouteDefinition` | `FromLocationId`, `ToLocationId`, `TravelMinutes`, **`BaseElevationLevel`** (S8, trừ vào flood/current band trước khi clamp — route cao ráo ít ngập hơn) | 🟡 |
| `Assets/Game/Data/Definitions/SearchPointDefinition.cs` | `SearchPointDefinition` + `LootEntry` | `LocationId`, `OpenTimeMinutes` (mặc định 0 — search mở tức thì), `LootTable` (List\<LootEntry\>: ItemId/**Guaranteed**/**Chance**(0-100)/MinQuantity/MaxQuantity — hybrid 2026-07-24: Guaranteed luôn spawn, còn lại roll theo Chance; **`Weight` đã xoá** — knob cũ chết, mọi entry luôn spawn bất kể giá trị) | 🟡 |
| `Assets/Game/Data/Definitions/ShelterZoneDefinition.cs` (S10) | `ShelterZoneDefinition` | `Floor` ("Ground"/"Upper"/"Roof"), `BuildSlotIds:List<string>`, `WaterRisk` (display-only, chưa rule nào đọc). Giả định 1 shelter duy nhất (không có field ShelterId) — S17 mới cần tách khi thêm shelter thứ 2. Content `shelterzones_p3.json`: 8 zone đúng main-shelter-design.md §6-14 | 🟡 (qua ContentValidationTests) |
| `Assets/Game/Data/Definitions/ModuleDefinition.cs` (S11) | `ModuleDefinition` | `AllowedZoneIds:List<string>` (zone id được xây), `Materials:Dict<itemId,qty>`, `BuildMinutes`, `PowerDemand` (đọc thật từ S12), `MaxDurability`, `Tags` (vd "pump"/"barrier" — rule code đọc tag, không so string Id). Content `modules_p3.json`: 5 module (Barrier/Pump/Elevated Storage/Purifier/Battery Bank) đúng bảng baseline plan | 🟡 (qua ContentValidationTests) |
| `Assets/Game/Data/Definitions/EventDefinition.cs` (S13; +S14; +S16) | `EventDefinition` | Trigger: `TriggerPhaseId`/`TriggerRequiresBlackWater`/`TriggerStateMinLevel`/`TriggerRequiresPumpModule`/`TriggerChancePercentPerLongTick` (AND phẳng, KHÔNG phải cây Compound Trigger đầy đủ). S14: `SoftDeadlineMinutes`/`HardDeadlineMinutes` (tính từ lúc thành Active), `RequiresDiscovery` (instance khởi đầu Undiscovered), `NextEventId` (Event Chain), `ExpirationShelterFlags`/`ExpirationPersistentFlags` (Persistent Consequence). **S16: `DiscoveryLocationId`** (RequiresDiscovery event có thể yêu cầu đúng 1 location cụ thể thay vì mặc định "bất kỳ shelter nào" — vd gặp NPC ở location của họ) + **`NextEventIdByResponse:Dict<responseId,eventId>`** (chain rẽ nhánh theo response người chơi chọn khi resolve — fallback `NextEventId` nếu response không có entry riêng; expire LUÔN dùng `NextEventId`, không bao giờ theo response vì expire không có response). `Priority` điều khiển sleep-wake (Critical/Major/Standard); `EventType`/`Scope` vẫn chỉ hiển thị, chưa có Event Budget. Content: `events_p3.json` (3 event) + `events_p4_minh.json` (S16, 5 event — chain Nguyễn Minh) + **`events_p4.json` (S17, 4 event slice chính)**: Storm Warning (Major, trigger `phase_p4_warning`), Black Rain Transition (Critical, trigger `phase_p4_black_rain`), School Rescue (Major, RequiresDiscovery tại `location_school`, soft 60'/hard 120', response rescue/leave, hết hạn → flag `school_survivor_lost`), Grid Failure (Critical, tag mới **"grid_failure"** — `EventSystem.Trigger()` set `PersistentFlags["grid_down"]=true` ngay lúc trigger, `PowerSystem` đã đọc flag này sẵn từ S12 nên không cần sửa PowerSystem). Drain Backflow/Pump Jam/Storage Flood (S13) KHÔNG cần re-tune — đã phase-agnostic (đọc `TriggerRequiresBlackWater`/`TriggerStateMinLevel`, không hardcode phase id) | 🟡 (qua ContentValidationTests + EventLifecycleTests + SliceTimelineTests) |
| `Assets/Game/Data/Definitions/NpcDefinition.cs` (S15; content S16) | `NpcDefinition` | `DisplayName`, `Skill` ("electric" — SkillMultiplier chưa ai đọc, để dành S17 khi task assignment tính thời gian theo skill), `SkillMultiplier`, `StartingTrust`, `StartingLocationId`. Content `npcs_p4.json` (S16): `npc_minh` (Nguyễn Minh, trust 30, tại `location_convenience_store`) | 🟡 (qua ContentValidationTests + NpcCommandsTests) |
| `Assets/Game/Core/Commands/NpcCommands.cs` (S16) | `RecruitNpcCommand`, `AssignNpcTaskCommand` | Recruit: validate definition tồn tại + chưa recruited (`NpcUnavailable`) + player ở shelter (`NotAtLocation`) + còn chỗ `LivingCapacity` (`CapacityFull`) — tạo/reset `NpcState` (Health=Healthy, Trust=StartingTrust, LocationId=vị trí player lúc recruit), `Occupants++`, **S18: `DecisionLog.Append`**, publish `NpcRecruited`+`NpcStateChanged`. **Cố tình KHÔNG gate theo flag tường thuật** (vd "đã gặp Minh chưa") — đó là việc UI/content (nút chỉ hiện sau khi gặp), không phải validate cơ chế. AssignNpcTask: validate task tồn tại (`TaskNotFound`) + `Kind==Active` (`InvalidState`) + npc đã recruited (`NpcNotRecruited`) — set `ActiveTaskState.RequiredWorker` sang npc id (lần đầu tiên nhánh Active task của `TaskSystem` có consumer thật, dù chưa có content nào tạo Active task) | ✅ (NpcCommandsTests, 9 test) |
| `Assets/Game/Core/Commands/DecisionLog.cs` (S18) | `DecisionLog` (static) | `Append(ctx, decisionId, payload)` — ghi `WorldState.DecisionLog` (Causal Report source). Gọi từ đúng 4 chỗ plan gốc liệt kê: `ResolveEventCommand`, `StartBuildCommand`, `RecruitNpcCommand`, `EvacuateCommand` | ✅ (gián tiếp qua SliceRoundtripTests) |
| `Assets/Game/Core/Commands/EvacuateCommand.cs` (S18) | `EvacuateCommand` | Validate: player đang ở location của main shelter (`Balance.NewGame.StartLocationId`, `NotAtLocation` nếu không) + shelter đã `GroundFloorLost` (`InvalidState` nếu chưa mất — "chưa có gì để evacuate"). Execute: xoá sạch `ShelterState.Storage.Items` ("bỏ storage lại" đúng plan gốc), set `PersistentFlags["evacuated"]=true`, `DecisionLog.Append`, publish `EvacuationDeclared`. **KHÔNG tự di chuyển player** — người chơi tự đi `location_school` bằng `BeginTravelCommand` có sẵn (S17 cắt "Temporary Shelter nâng cấp" nên không có shelter thứ 2 để "đổi shelter chính" theo đúng nghĩa plan gốc — evacuate ở đây = cam kết + bỏ đồ, `OutcomeRules` xác nhận "tới nơi" bằng vị trí cuối cùng) | ✅ (qua SliceRoundtripTests + OutcomeRulesTests) |
| `Assets/Game/Core/Rules/OutcomeRules.cs` (S18) | `OutcomeRules` (static) | `Evaluate(world, mainShelter, cfg) → Outcome{StableSurvival,ForcedEvacuation,Collapse}` — thuần, không GameContext. Collapse nếu `ConditionOps.IsIncapacitated` (không có state "chết" riêng cho player). Nếu shelter `GroundFloorLost`: có cờ `evacuated` VÀ đang đứng ở `SliceBalance.EvacuationLocationId` → ForcedEvacuation, ngược lại Collapse. Nếu shelter còn ổn: `WaterStocks.Clean >= MinCleanWaterForStableSurvival` → StableSurvival, dưới ngưỡng → Collapse (sống sót nhưng thiếu tài nguyên KHÔNG tính ending tốt — theo đúng "≥ tài nguyên tối thiểu" là điều kiện bắt buộc trong plan gốc, không phải tuỳ chọn) | ✅ (OutcomeRulesTests, 8 test) |
| `Assets/Game/Systems/Outcome/OutcomeSystem.cs` (S18) | `OutcomeSystem` (plain C#) | LongTick, đánh giá đúng 1 lần (`PersistentFlags["outcome_reached"]`, resync qua `WorldStateReloaded` — save/load giữa chừng không đánh giá lại/không mất). Điều kiện: player Incapacitated (fail tức thời) HOẶC đã tới phase CUỐI CÙNG trong `DisasterPhasesSorted` (đọc động từ content, không hardcode "phase_p4_end") → `OutcomeRules.Evaluate` → publish `OutcomeReached`. Không dùng `RegisterThreshold` (cùng lý do EventSystem S14 — không serialize) | ✅ (SliceRoundtripTests, 3 test) |
| `Assets/Game/UI/Outcome/OutcomeReportPanel.cs` (S18) | `OutcomeReportPanel` | Tự mở khi nghe `OutcomeReached` (không cần phím riêng, giống `WorldMapPanel.Open()` qua event), Esc đóng. Hiện: tên Outcome (header), Major Decisions (toàn bộ `WorldState.DecisionLog`), Resources Preserved (Clean/Untreated Water, số Module), Shelter Outcome (WaterIntrusion Level), NPC Outcome (mỗi NPC recruited: tên/Health/Trust). Cùng pattern code-built Canvas/TMP như EventPanel | ⬜ (chưa test tự động, chỉ verify compile + wiring scene) |
| `Assets/Game/Systems/Npc/NpcSystem.cs` (S16) | `NpcSystem` (plain C#) | LongTick: mỗi NPC Recruited & chưa Dead — Hunger/Thirst cộng dồn theo `NpcBalance` (rời rạc: chỉ "ăn/uống" khi mét chạm 100, không mô phỏng liên tục như player); chạm 100 → thử tiêu thụ (nước từ `ShelterState.WaterStocks.Clean`, đồ ăn = item đầu tiên trong `ShelterState.Storage` có `ItemDefinition.Category=="food"`, giống cách `RestAtShelterCommand` tìm item "medical") — thành công: reset mét + Trust+; thiếu: Trust−, đếm `NpcState.StarvingLongTicks`, đủ ngưỡng → `DowngradeHealth`. Ngập: NPC đang ở 1 location `IsShelter` + `WaterIntrusion.Level>=Deep` → đếm `FloodExposureLongTicks`, đủ ngưỡng → `DowngradeHealth` (CHUNG hàm với nhánh đói, không phải 2 hệ thống riêng). `DowngradeHealth` chạm Dead → trừ `Occupants`, publish `NpcDied` | ✅ (NpcSystemTests, 11 test) |
| `Assets/Game/Data/BalanceConfig.cs` (S5; +`ConditionBalance` S7; +`HazardBalance` S8; +`ShelterBalance` S10) | `BalanceConfig`, `InventoryBalance`, `TravelBalance`, `NewGameBalance`, `ConditionBalance`, `HazardBalance`, `ShelterBalance` | Object config duy nhất — capacity, overload, speed modifier, travel load factor, start location (**+`MainShelterId` S10, mặc định "shelter_main"**), condition (thirst/hunger/fatigue/stamina/body-temp/wet/cold/exposure/starvation/collapsed + shelter_rest/treat_exposure S9), hazard (crossing stamina/exposure/wet/time theo tier, contaminated handling exposure), **shelter (S10): threshold Damp/Shallow/Deep/Critical, `InflowByRainIntensity[]` (index theo `DisasterPhaseDefinition.RainIntensity`), backflow/passive-drain/pump-output, initial structural/living-capacity/water** | ✅ (BalanceLoadTests) |
| `Assets/Game/Data/DefinitionRegistry.cs` | `DefinitionRegistry` | `DefinitionVersion`, `Balance`, `Items/Locations/Routes/SearchPoints/DisasterPhases/ShelterZones/Modules/**Events**` (S10/S11/S13, IReadOnlyDictionary), **`DisasterPhasesSorted:IReadOnlyList`** (S8, sort 1 lần theo `StartMinute` lúc construct — `DisasterPhaseSystem`/`HazardSystem`/`ReturnWindowCalculator`/`BeginTravelCommand` đều dùng chung list này, không tự sort riêng), `TryGetItem/Location/Route/SearchPoint/DisasterPhase/ShelterZone/Module/Event`. Ctor: `disasterPhases`/`shelterZones`/`modules`/`events` param optional (default empty dict) | ✅ (qua DefinitionLoaderTests) |
| `Assets/Game/Data/DefinitionLoader.cs` | `DefinitionLoader` (static) | `Load(directoryPath) → DefinitionLoadResult{Success,Registry,Errors}`. Routing theo prefix file: `manifest.json`, `items_*.json`, `locations_*.json`, `routes_*.json`, `searchpoints_*.json`, `balance.json` (S5), `phases_*.json` (S7 — rỗng hợp lệ; nếu có thì bắt buộc unique `start_minute` + có phase tại phút 0), `shelterzones_*.json` (S10, không validation dangling ref riêng), `modules_*.json` (S11 — `ValidateModules`: Materials phải tham chiếu item có thật, AllowedZoneIds phải tham chiếu zone có thật), **`events_*.json`** (S13, không validation dangling ref riêng — response id chỉ là string tự do, chưa có registry response). Gom TOÀN BỘ lỗi (duplicate id, dangling ref, missing id) — không fail-first | ✅ |

## LastHope.Systems

| File | Class | API chính | Test |
| --- | --- | --- | --- |
| `Assets/Game/Systems/Registry/GameServiceRegistry.cs` | `GameServiceRegistry` (static) | `Register<T>`, `Get<T>`, `TryGet<T>`, `Clear()` — service locator giới hạn, chỉ `GameBootstrapper` ghi | ⬜ |
| `Assets/Game/Systems/Boot/GameBootstrapper.cs` | `GameBootstrapper` (MonoBehaviour, sống trong `10_GamePersistent`) | Composition root: load Definitions từ `StreamingAssets/Definitions`, fail-fast nếu lỗi (dừng boot, `enabled=false`), tạo `WorldState` mới + seed, set `Player.CurrentLocationId = Balance.NewGame.StartLocationId` (S5), dựng toàn bộ Core service + `InventorySystem` (S5) + `DisasterPhaseSystem`, `ConditionSystem` (S7) + `HazardSystem` (S8) + `WaterIntrusionSystem` (S10) + `TaskSystem` (S11) + `PowerSystem`/`WaterSystem` (S12) + **`EventSystem`** (S13), đăng ký vào `GameServiceRegistry` | ⬜ (verify qua headless smoke test, chưa có PlayMode test) |
| `Assets/Game/Systems/Boot/SimulationDriver.cs` | `SimulationDriver` (MonoBehaviour) | Cầu nối Unity Time → Core: đọc service ở `Start()` (không phải `Awake()`, tránh phụ thuộc thứ tự component), `Update()` clamp delta 1s, gọi `SimulationClock.AccumulateRealSeconds` + `TickScheduler.Advance`. `DebugPaused`/`DebugTimeScale` cho tooling | ⬜ (verify qua headless smoke test 10s không exception) |
| `Assets/Game/Systems/Inventory/InventorySystem.cs` (S5) | `InventorySystem` (plain C#) | Nghe `InventoryChanged` (chỉ owner "player") → `InventoryRules.ComputeOverload` → nếu đổi thì set `Inventory.Overload` + publish `OverloadStateChanged`. `RecomputeAll()` gọi 1 lần lúc boot | ✅ (qua InventoryRulesTests) |
| `Assets/Game/Systems/Disaster/DisasterPhaseSystem.cs` (S7) | `DisasterPhaseSystem` (plain C#) | Sắp `DisasterPhases` theo `StartMinute`; construct + nghe `WorldStateReloaded` → resync (recompute phase hiện tại từ `WorldTimeMinutes` + đăng ký lại threshold tương lai qua `ctx.Clock.RegisterThreshold`). `TransitionTo` idempotent (no-op nếu phase không đổi) nên double-register threshold sau reload vô hại | ✅ (DisasterPhaseSystemTests, 5 test) |
| `Assets/Game/Systems/Condition/ConditionSystem.cs` (S7) | `ConditionSystem` (plain C#) | ShortTick: stamina regen (×0.5 khi status `black_water_exposure`/`sick`), wet gain khi mưa ngoài trời (**S9: nhân thêm `EquipmentRules` wet_multiplier — jacket giờ CHỐNG ĐƯỢC mưa cả lúc đứng yên, không chỉ lúc crossing route**)/khô tại shelter, body temp drift, cold status hysteresis. LongTick: hunger/thirst/fatigue accrual, **S9: nếu `PlayerConditionState.TreatingExposure` → trừ thêm exposure trước khi tính chain**, `ConditionOps.ApplyExposureStatusChain`, health decay khi đói/khát cạn (floor 1) + khi sick (không floor). Nghe `TravelCompleted` → +fatigue cố định. Nguồn "black_water" exposure thật (S8): `BeginTravelCommand`, `TransferItemCommand` | ✅ (ConditionSystemTests 9 + ScenarioTests B 2 test) |
| `Assets/Game/Systems/Hazard/HazardSystem.cs` (S8) | `HazardSystem` (plain C#) | Mỗi long-tick + `WorldStateReloaded`: tính lại `RouteState` cho MỌI route qua `HazardRules.EvaluateRoute`, publish `RouteStateChanged` CHỈ khi Flood/Current/Contamination/Closed đổi. Đây CHỈ là cache hiển thị cho World Map — có thể trễ tới 9 phút; validate travel thật nằm ở `BeginTravelCommand` (tự tính fresh, không đọc RouteState) | ⬜ (verify gián tiếp qua TravelTests + headless smoke) |
| `Assets/Game/Systems/Telemetry/TelemetryLogger.cs` (S6) | `TelemetryLogger` | JSONL `persistentDataPath/Telemetry/session_*.jsonl` (`File.AppendAllText`, không giữ file handle). Nghe `TravelStarted/Completed` (kèm carry load lúc về), `SearchPointOpened`, `ItemTransferred` (chỉ log khi đích = player → `item_collected`). `Log()` public cho UI gọi thêm (chưa dùng — `item_left_behind`/`inventory_open_time` để dành cho `ContainerPanel`/`InventoryPanel` sau) | ✅ (TelemetryTests) |
| `Assets/Game/Systems/Shelter/WaterIntrusionSystem.cs` (S10; +Pump/Barrier S11; +Backflow/Jam flag S13) | `WaterIntrusionSystem` (plain C#) | Chỉ 1 shelter (`Balance.NewGame.MainShelterId`, mặc định "shelter_main"). Construct + `WorldStateReloaded` → `Resync()`: tạo `ShelterState` nếu chưa có, seed từ `ShelterBalance.Initial*` (chỉ khi `StructuralIntegrity==0`, tránh ghi đè save đã có), đảm bảo `BuildSlots` đủ mọi slot id từ `ShelterZoneDefinition`. LongTick: đếm module Active có tag "pump"/"barrier" trong `shelter.Modules` (S11), **đọc `ShelterEventFlags.PumpJammed` → nếu có thì đếm pump=0 bất kể module Active hay không (S13, Pump Jam event)**, đọc `ShelterEventFlags.DrainBackflowActive` cho `backflowActive` (S13, trước đó luôn `false`) → `WaterIntrusionRules.ComputeDelta` → cộng dồn `Units`; Barrier active + Level>Dry → durability −2/long-tick, chạm 0 → tự `Active=false` (không còn chặn); publish `ShelterWaterChanged` CHỈ khi `Level` đổi; Deep→set flag `lower_floor_power_locked`, Critical→thêm `ground_floor_lost` (tự xoá khi hồi phục) | ✅ (WaterIntrusionSystemTests 6 + BuildCommandsTests 1 tích hợp pump + EventCommandsTests tích hợp backflow) |
| `Assets/Game/Systems/Tasks/TaskSystem.cs` (S11; +S16) | `TaskSystem` (plain C#) | LongTick: mỗi `ActiveTaskState` Running — Passive luôn tiến (kể cả FastForward lúc vắng/ngủ, vì LongTick chạy đều bất kể nguồn gốc); Active cần `RequiredWorker` đang ở shelter — **S16: `IsWorkerAtShelter` tổng quát hoá, nhận cả npc id** (trước chỉ hardcode player) qua `AssignNpcTaskCommand`; vẫn chưa có content nào tạo Active task (Purify batch dùng validate-rồi-FastForward thay vì Active task) — cơ chế đã mở khoá, chờ content dùng tới. Chỉ Build task (`ModuleId` khác null) có công thức tiến độ: `+= 100×10/BuildMinutes` mỗi long-tick, publish `BuildProgressChanged`; đạt 100% → tiêu thụ `task:<id>` inventory, tạo `ModuleState` (Active=true ngay), gán `BuildSlots[slotId].ModuleInstanceId`, **S12: nếu module có PowerDemand>0 thì gán priority mặc định Normal vào `ShelterPowerState.Priorities`**, xoá task, publish `ModuleCompleted`+`TaskStateChanged` | ✅ (BuildCommandsTests, gián tiếp qua toàn bộ luồng Build) |
| `Assets/Game/Systems/Shelter/PowerSystem.cs` (S12) | `PowerSystem` (plain C#) | LongTick: gom demand từ mọi module có `PowerDemand>0` (đọc priority từ `ShelterPowerState.Priorities`, mặc định Normal nếu thiếu) → `PowerRules.Allocate` (Grid đọc qua flag `WorldState.PersistentFlags["grid_down"]` — chưa ai set tới S17) → set `ModuleState.Active` theo kết quả (CHỈ module có demand, Barrier/Elevated Storage không đụng tới), publish `PowerStateChanged` nếu có đổi | ✅ (ShelterCommandsTests, tích hợp qua PowerRules) |
| `Assets/Game/Systems/Shelter/WaterSystem.cs` (S12) | `WaterSystem` (plain C#) | LongTick: CHỈ tích `WaterStocks.Untreated` thụ động khi đang mưa (`RainIntensity>0`), publish `WaterStocksChanged`. Batch Purify KHÔNG tự động — `StartPurifyBatchCommand` (người chơi chủ động) mới xử lý | ✅ (ShelterCommandsTests, 1 test riêng cho intake) |
| `Assets/Game/Systems/Events/EventSystem.cs` (S13; S14 lifecycle đầy đủ) | `EventSystem` (plain C#) | LongTick 2 pha. **Trigger pha**: mỗi `EventDefinition` — dedup theo EventId (Active/Undiscovered chặn tạm, Expired/PersistentConsequence chặn vĩnh viễn, chỉ Resolved cho trigger lại), roll RNG stream "events" nếu có chance gate, `EventTriggerRules.Evaluate` → tạo instance State=Active (hoặc Undiscovered nếu `RequiresDiscovery`), áp effect trigger-time theo Tag bất kể discovery (off-scene principle), publish `EventTriggered` (chỉ khi Active — event ẩn không lộ qua UI). **Lifecycle pha** (`AdvanceLifecycles`): Undiscovered + player đứng ở location `IsShelter` → Active + arm deadline từ lúc discovery + publish `EventDiscovered`; Active quá soft deadline → publish `EventDeadlineApproaching` 1 lần; quá hard deadline → `Expire` (áp `ExpirationShelterFlags`/`ExpirationPersistentFlags`, State=PersistentConsequence nếu có flag/Expired nếu không, publish `EventExpired`, chain `NextEventId`). Subscribe `EventResolved` → chain `NextEventId` (force-trigger bỏ qua trigger conditions, dedup vẫn áp). Deadline check mỗi LongTick thay vì `RegisterThreshold` (threshold không serialize — mất khi save/load). Off-scene: không đọc vị trí player trừ discovery check | ✅ (EventCommandsTests + EventLifecycleTests 15 test) |
| `Assets/Game/Systems/Intel/IntelSystem.cs` (S15) | `IntelSystem` (plain C#) | Quan sát trực tiếp = intel Confirmed. Subscribe `TravelCompleted` → ghi intel route vừa đi + location đến; `SearchPointOpened` → ghi intel location của search point đó. `RecordRouteObservation`/`RecordLocationObservation` là **static** (không chỉ instance method) để `WorldMapPanel` cũng gọi được trực tiếp (đứng cạnh route = quan sát tươi mỗi lần mở map) mà không cần giữ reference tới `IntelSystem`. Ghi qua `Record()` dùng `IntelRules.ShouldReplace` (record mới hơn thắng), publish `IntelUpdated`. Radio/comms (intel Uncertain/Reliable KHÔNG quan sát trực tiếp) chờ S16+ khi có nguồn radio | ✅ (IntelSystemTests, 5 test) |

## LastHope.DebugTools (bổ sung S4)

| File | Class | API chính | Test |
| --- | --- | --- | --- |
| `Assets/Game/DebugTools/Panel/DebugPanel.cs` | `DebugPanel` (MonoBehaviour, OnGUI, F2) | Xem World Time, Fast-forward clock, Pause/TimeScale, Condition cheat (S7), Equipment (S8, qua command thật), **Rest at Shelter** (S9: nút Rest/Treat Exposure/Dry Off → `RestAtShelterCommand` thật, không bypass), **Sleep (S12)**: ô nhập phút + nút Sleep → `StartSleepCommand` thật (không bypass — test được cả interrupt-khi-ngập), **Shelter (S10)**: xem Structural/Water Intrusion/WaterStocks/EventFlags/Modules/ActiveTasks + nút "Add Water Units" (bypass, set trực tiếp `WaterIntrusion.Units` qua `WaterIntrusionRules`), **Events (S13; +S14)**: nút "Force-trigger" mỗi `EventDefinition` (bypass `EventTriggerRules`, arm cả soft+hard deadline) + liệt kê event Active **và Undiscovered** (debug thấy cả event ẩn — player UI thì không) kèm nút theo từng `AvailableResponses` → `ResolveEventCommand` thật, **Phase jump cheat** (S9: nút nhảy thẳng tới từng phase trong `DisasterPhasesSorted`, set `WorldTimeMinutes` rồi publish `WorldStateReloaded` để `DisasterPhaseSystem`/`HazardSystem`/`WaterIntrusionSystem` resync), Add Item (bypass, **S10+DebugPanel fix 2026-07-24: giờ auto-fill instance id + slot vào ô Equip**), Travel cheat, Save/Autosave + Load theo slot, state tree dump. **Fix 2026-07-24**: toàn bộ nội dung bọc trong `GUILayout.BeginScrollView` ngoài — trước đó `BeginArea` cao cố định 640px clip cả hình lẫn input phần cuối panel | ⬜ (chưa test tự động, chỉ verify code compile + wiring scene) |

## LastHope.Presentation

| File | Class | API chính | Test |
| --- | --- | --- | --- |
| `Assets/Game/Presentation/Camera/CameraRig.cs` | `CameraRig` | **S19 (2026-07-25): viết lại cho 2D**, **2026-07-26: xóa và viết lại lần 2 (S1-S4-redo, GUID đổi)** — orthographic 2D cố định, KHÔNG xoay (bỏ pitch/yaw 3D), `transparencySortMode=CustomAxis` (Y-sort) + `transparencySortAxis` (tỉ lệ diamond iso, tune khi có tile thật). Zoom qua Input System action "Zoom". `SetTarget(Transform)`, `SetInputActions(InputActionAsset)` | ⬜ (chỉ headless smoke test, chưa unit test) |
| `Assets/Game/Presentation/Player/PlayerController.cs` | `PlayerController` | **S19: viết lại cho 2D**, **2026-07-26: xóa và viết lại lần 2 (S1-S4-redo, GUID đổi)** — `Rigidbody2D` kinematic, `MovePosition` trong `FixedUpdate`. Input Vector2 map thẳng world X/Y (camera không xoay nên world=screen luôn). Bỏ hẳn gravity/fall-recovery (`fallResetY`) — không còn khái niệm rơi khỏi map trong top-down 2D. `SpeedModifier` (S5: nay được `PlayerAvatarSync` set theo `OverloadStateChanged`), `SetInputActions` | ⬜ |
| `Assets/Game/Presentation/Player/PlayerAvatarSync.cs` (S5) | `PlayerAvatarSync` | **Presentation-write exemption** có chủ đích: `LateUpdate` ghi transform→`PlayerState.PositionX/Y` mỗi frame. **S19: `Rigidbody2D` thay `CharacterController`, `PositionZ` xoá khỏi `PlayerState`** (2D chỉ X/Y). **2026-07-26: xóa và viết lại (S1-S4-redo, GUID đổi)**, nội dung tương đương. KHÔNG tự ghi `PositionLocationId` (fix bug rơi-khỏi-map 2026-07-24 — field này chỉ được `SceneFlowController` stamp sau khi đặt spawn xong, tránh race). Nghe `WorldStateReloaded` → teleport lại nếu `PositionLocationId==CurrentLocationId` (khác thì để yên, `SceneFlowController` lo). Nghe `OverloadStateChanged` → set `PlayerController.SpeedModifier` | ⬜ |
| `Assets/Game/Presentation/Interaction/IInteractable.cs` (S5) | `IInteractable` | `PromptText`, `CanInteract(ctx)`, `Interact(ctx,processor)` — tương tác tức thì (E), docs không spec hold-duration | ⬜ |
| `Assets/Game/Presentation/Interaction/InteractionDetector.cs` (S5) | `InteractionDetector` | **S19: `Physics2D`** — `OverlapCircleNonAlloc` bán kính 1.6m mỗi 0.15s + cursor tiebreak (`ScreenToWorldPoint`+`Collider2D.OverlapPoint`, ưu tiên object con trỏ trỏ vào). Đọc action "Interact" có sẵn, `Current`/`TargetChanged`. S6: nay có interactable thật (SearchPointView×6, ShelterStorageView, TravelPointView×2) | ⬜ |
| `Assets/Game/Presentation/Interaction/InteractionPrompt.cs` (S5) | `InteractionPrompt` | TextMeshProUGUI "E — {prompt}", nghe `InteractionDetector.TargetChanged` | ⬜ |
| `Assets/Game/Presentation/World/SearchPointView.cs` (S6) | `SearchPointView` (`IInteractable`) | `[SerializeField] searchPointId` bind với `SearchPointDefinition`. Interact → `OpenSearchPointCommand`. **Awake() spawn `WorldLabel`** (2026-07-24, hiện "Search\n<id>" phía trên) | ⬜ |
| `Assets/Game/Presentation/World/ShelterStorageView.cs` (S6) | `ShelterStorageView` (`IInteractable`) | `[SerializeField] shelterId`. Interact → publish `ContainerViewRequested` trực tiếp. **+`WorldLabel`** ("Storage") | ⬜ |
| `Assets/Game/Presentation/World/TravelPointView.cs` | `TravelPointView` (`IInteractable`) | S8: Interact → publish `WorldMapRequested`, không còn giữ `routeId` — `WorldMapPanel` tự đọc `LocationDefinition.ConnectedRouteIds` của vị trí hiện tại. **+`WorldLabel`** ("Travel") | ⬜ |
| `Assets/Game/Presentation/World/WorldLabel.cs` (2026-07-24) | `WorldLabel` | Text nổi world-space (TextMeshPro 3D, không phải UGUI). **S19: bỏ facing-rotation** (camera 2D không xoay nên `Quaternion.identity` luôn đọc đúng, không cần tính lại mỗi frame). `WorldLabel.Create(transform, text)` — dùng để phân biệt sprite marker blockout giống hệt nhau sau phản hồi playtest "không nhận dạng được vật thể" | ⬜ |
| `Assets/Game/Presentation/World/PlayerSpawnPoint.cs` (S6) | `PlayerSpawnPoint` | Marker rỗng — `SceneFlowController` dùng khi không có vị trí save khớp scene mới | ⬜ |
| `Assets/Game/Presentation/World/BuildSlotView.cs` (S10) | `BuildSlotView` | `[SerializeField] slotId` + `WorldLabel`. KHÔNG interactable — S11's BuildPanel đọc `ShelterState.BuildSlots` trực tiếp, không qua world interaction | ⬜ |
| `Assets/Game/Presentation/World/CoreComponentView.cs` (S10) | `CoreComponentView` | Marker cho 5/6 Fixed Core Component chưa có tương tác (Staircase/Pillars/ElectricalBackbone/WaterIntake/AntennaMount) — chỉ `WorldLabel`, không `IInteractable` | ⬜ |
| `Assets/Game/Presentation/World/DrainCoreView.cs` (S10) | `DrainCoreView` (`IInteractable`) | Core Component duy nhất có tương tác thật trong S10: đọc `ShelterState.WaterIntrusion` qua `GameLog`, không mutate state (Clean/Valve/Seal/Pump chờ S11 Build System) | ⬜ |
| `Assets/Game/Presentation/Boot/BootLoader.cs` | `BootLoader` (MonoBehaviour, sống trong `00_Boot`) | S6: chỉ load `10_GamePersistent` additive — KHÔNG còn hard-code scene gameplay thứ 2 (đó là việc của `SceneFlowController`) | ⬜ |
| `Assets/Game/Presentation/Boot/GamePersistentMarker.cs` | `GamePersistentMarker` | `DontDestroyOnLoad` cho root scene `10_GamePersistent`, chặn instance thứ 2 | ⬜ |
| `Assets/Game/Presentation/Boot/SceneFlowController.cs` (S6) | `SceneFlowController` | Chủ sở hữu DUY NHẤT vòng đời scene gameplay: nghe `TravelCompleted`/`WorldStateReloaded` → load additive scene theo `LocationDefinition.SceneName` của `Player.CurrentLocationId`, unload scene cũ, đặt player ở `PlayerSpawnPoint` nếu `PositionLocationId != CurrentLocationId`. **S19: đọc `Rigidbody2D` thay `CharacterController`** khi đặt lại vị trí. **Là nơi DUY NHẤT được phép stamp `player.PositionLocationId = CurrentLocationId`** sau khi đặt xong (fix bug rơi-khỏi-map — xem PlayerAvatarSync). Log ở mỗi bước, verify qua headless smoke | ⬜ (verify qua smoke test, chưa PlayMode test) |

## LastHope.UI

| File | Class | API chính | Test |
| --- | --- | --- | --- |
| `Assets/Game/UI/Inventory/InventoryPanel.cs` (S5; **fix lifecycle 2026-07-24**) | `InventoryPanel` | Panel code-built hoàn toàn: list phẳng item + 2 thanh weight/volume màu theo Overload, nút Use. **Drop vẫn chưa làm**. Toggle phím I/Tab dùng `CanvasGroup` (không `SetActive`) — `SetActive(false)` từ `Update()` của chính GameObject sẽ khiến `Update()` không chạy nữa, không bao giờ mở lại được; action resolve ở `Awake()` (không phải `Start()`) vì `OnEnable()` chạy trước `Start()` | ⬜ |
| `Assets/Game/UI/Container/ContainerPanel.cs` (S6; **fix lifecycle + Escape + exclusivity 2026-07-24**) | `ContainerPanel` | 1 panel dùng chung search point + shelter storage. Nghe `ContainerViewRequested` → hiện list (đọc qua `InventoryOwnerResolver`, mutate qua `TransferItemCommand`): Take/Take All, +Store nếu owner `shelter_storage:`. **Bug lifecycle đã sửa:** `Awake()` từng tự `SetActive(false)` → `Start()` chưa từng chạy → panel chưa từng mở ra từ S6. Giờ có thêm: `inputActions` field (mới, trước không có) + phím **Esc** đóng panel (action "Close", resolve ở `Awake()`); publish/nghe `ExclusivePanelOpened` — tự đóng khi `WorldMapPanel` mở, và ngược lại (tránh chồng chữ không đọc được) | ⬜ |
| `Assets/Game/UI/Hud/ConditionHud.cs` (S9) | `ConditionHud` | HUD luôn hiện (không toggle) — 4 thanh Health/Stamina/Hunger/Thirst + dòng badge status (`StatusEffects` keys + "collapsed" nếu Incapacitated). Rebuild khi nghe `ConditionChanged`. Đây là bản player-facing của Condition section trong DebugPanel (S7 làm trước để verify bằng F2 trước khi xây UI thật) | ⬜ (chưa test tự động, chỉ verify code compile + wiring scene) |
| `Assets/Game/UI/Map/WorldMapPanel.cs` (S8; **fix lifecycle + Escape + exclusivity 2026-07-24**; **S15: chỉ render intel**) | `WorldMapPanel` | Nghe `WorldMapRequested` (từ `TravelPointView`) hoặc phím **M** trực tiếp; phím **Esc** đóng. Liệt kê `LocationDefinition.ConnectedRouteIds` của vị trí hiện tại — **S15: không còn đọc `HazardRules`/`TravelRules`/`ReturnWindowCalculator` trực tiếp**, chỉ render từ `WorldState.Intel.Records` (Flood/Current/Closed + `[Confidence, tuổi thông tin]` qua `IntelRules.EffectiveConfidence`); route chưa có record hiện "?". Route đang kết nối vị trí hiện tại được `IntelSystem.RecordRouteObservation(...Confirmed)` refresh ngay mỗi lần `Rebuild()` — "đứng cạnh thì luôn biết mới nhất". Nút Travel vẫn submit `BeginTravelCommand` thật (command tự validate live state, intel chỉ hiển thị). **Viết lần đầu (S8) dính nguyên 3 lỗi lifecycle giống InventoryPanel/ContainerPanel** — đã sửa: `CanvasGroup` thay `SetActive`, action resolve ở `Awake()`. Publish/nghe `ExclusivePanelOpened` | ⬜ (chưa test tự động, chỉ verify code compile + wiring scene) |
| `Assets/Game/UI/Shelter/BuildPanel.cs` (S11) | `BuildPanel` | Phím **B** toggle, **Esc** đóng, tham gia `ExclusivePanelOpened` — copy nguyên pattern lifecycle-safe của WorldMapPanel (CanvasGroup, action resolve `Awake()`) thay vì viết lại, tránh tái phạm bug 2026-07-24. Liệt kê mọi `ShelterState.BuildSlots`: Locked → hiện "Locked"; có Module → hiện durability/Active + nút Dismantle; có task đang chạy → hiện Progress% + Pause/Resume/Cancel; trống → liệt kê nút "Build {module}" cho từng `ModuleDefinition` khớp zone (qua `BuildRules.TryFindZoneForSlot`). Rebuild khi `TaskStateChanged`/`BuildProgressChanged`/`ModuleCompleted` | ⬜ (chưa test tự động, chỉ verify code compile + wiring scene) |
| `Assets/Game/UI/Events/EventPanel.cs` (S14) | `EventPanel` | Phím **V** toggle, **Esc** đóng, cùng pattern lifecycle-safe. List event **Active** (ẩn Undiscovered có chủ đích): tên + `[Priority]` + countdown hard deadline, nút theo từng `AvailableResponses` → `ResolveEventCommand` — event giờ resolve được trong UI người chơi, không cần F2. Bên dưới: History tối đa 6 event đã kết thúc (resolved kèm response/expired). Rebuild khi Event* events + mỗi `WorldTimeChanged` lúc đang mở (countdown) | ⬜ (chưa test tự động, chỉ verify compile + wiring scene) |
| `Assets/Game/UI/Events/EventToast.cs` (S14) | `EventToast` | Banner top-center, queue thông báo 4s/cái: `EventTriggered` ("Event:")/`EventDiscovered` ("Discovered:")/`EventDeadlineApproaching` ("Deadline approaching:")/`EventExpired` ("Too late:")/`EventResolved` ("Resolved:"). Không nhận input, CanvasGroup alpha ẩn/hiện. Event Undiscovered không bao giờ lộ ở đây (EventSystem không publish EventTriggered cho chúng) | ⬜ |
| `Assets/Game/UI/Shelter/ShelterPanel.cs` (S12; +S16) | `ShelterPanel` | Phím **N** toggle, **Esc** đóng, cùng pattern lifecycle-safe. Hiện Battery/Clean/Untreated + Flood (WaterIntrusion Level/Units) + **Occupants/LivingCapacity** tổng quan; mỗi module có `PowerDemand>0` → nút cycle priority qua `SetPowerPriorityCommand`; mỗi module tag "purifier" → nút "Purify Batch"; **S16: 1 dòng mỗi NPC đã recruited** (tên qua `NpcDefinition.DisplayName`, Health/Trust/Hunger/Thirst); nút "Collect 1" (Clean Water→bottle); dòng tóm tắt số active task. Rebuild khi `PowerStateChanged`/`WaterStocksChanged`/`ShelterWaterChanged`/`TaskStateChanged`/`NpcStateChanged` | ⬜ (chưa test tự động, chỉ verify code compile + wiring scene) |

## LastHope.DebugTools

| File | Class | API chính | Test |
| --- | --- | --- | --- |
| `Assets/Game/DebugTools/Overlay/DebugOverlay.cs` | `DebugOverlay` | OnGUI overlay, toggle **F1**: FPS, world position, build version. Tự tìm GameObject tag "Player" nếu chưa gán. **Chưa hiển thị World Clock/State** (đó là `DebugPanel` ở S4, phím F2). **2026-07-26: xóa và viết lại (S1-S4-redo, GUID đổi)** — 1 sửa thật: Pos hiện `(X, Y)` tường minh thay vì `Vector3.ToString` (trước in cả Z=0 gây hiểu lầm) | ⬜ |

## LastHope.EditorTools (Editor-only, không build vào Player)

| File | Class | API chính | Ghi chú |
| --- | --- | --- | --- |
| `Assets/Game/EditorTools/SceneSetup.cs` | `SceneSetup` | **S19 (2026-07-25): viết lại toàn bộ cho 2D isometric.** `[MenuItem] BuildAll()` — dựng lại 7 scene từ code, đăng ký Build Settings: `00_Boot`, `10_GamePersistent` (Player: `Rigidbody2D` kinematic + `CircleCollider2D` + sprite Visual, Camera+HUD Canvas: EventSystem, InputSystemUIInputModule, ConditionHud S9, InteractionPrompt, InventoryPanel, ContainerPanel, WorldMapPanel, BuildPanel, ShelterPanel, **EventPanel/EventToast S14**, SceneFlowController), `90_TestSystems` (Ground Tilemap + scale-ref sprite marker, không tự động load), `Shelters/20_MainShelter` (2 tầng biểu diễn bằng 2 vùng 2D tách biệt nối bằng marker "Stairs" đi bộ qua, không phải cao độ vật lý — zone/BuildSlot/CoreComponent), `Locations/41_Location_ConvenienceStore` (6 SearchPointView), `Locations/42_Location_UtilityGarage`/`43_Location_School` (S17, cùng pattern ConvenienceStore) — tái dùng `CreateSearchPoint`/`CreateBoundaryWalls`/`CreateGroundTilemap` chung. Ground = `Grid`(`CellLayout.Isometric`)+`Tilemap`, không collider (không phải obstacle). Prop/marker = `SpriteRenderer`+`BoxCollider2D` qua `CreateSpriteMarker` — **Zone/BuildSlot/Stairs cố tình KHÔNG có collider** (walkable area, khác 3D nơi collider mỏng không chặn `CharacterController` do chênh cao độ — `Collider2D` full-footprint 2D sẽ chặn thật nên phải bỏ). Sprite/tile placeholder màu đặc sinh 1 lần, lưu asset thật ở `Assets/Game/Generated/{Sprites,Tiles}` (qua `GetOrCreateSolidSprite`/`GetOrCreateSolidTile`, idempotent) — không phải runtime-only nên sống sót qua reload | Chạy lại bất cứ khi nào cần tái tạo scene từ đầu (deterministic). **Chưa chạy trong Unity Editor để xác nhận compile** (không có Editor trong môi trường AI) — cần user tự chạy |
| `Assets/Game/EditorTools/RenderPipelineSetup.cs` | `RenderPipelineSetup` | **S19: `Renderer2DData` thay `UniversalRendererData`** (URP 2D Renderer — Light2D/Shadow Caster 2D). `[MenuItem] Setup()` — tạo `Assets/Settings/LastHope_URP.asset` + Renderer, gán Graphics+Quality, Linear color space | Đã chạy 1 lần với renderer 3D cũ — **cần chạy lại sau S19** để renderer asset đổi sang `Renderer2DData` (không tự động migrate asset cũ) |
| `Assets/Game/EditorTools/BuildScript.cs` | `BuildScript` | `[MenuItem] BuildWindowsDevelopment()` → `Builds/Windows/LastHope.exe`, Mono, Development build | Dùng làm smoke test nhanh sau mỗi sprint |
| `Assets/Game/EditorTools/TmpSetup.cs` (S5) | `TmpSetup` | `[MenuItem] ImportEssentials()` — import "TMP Essential Resources.unitypackage" từ PackageCache (TMP Settings + LiberationSans SDF font). **Chạy KHÔNG kèm `-quit`** — `AssetDatabase.ImportPackage` là async, dùng callback `importPackageCompleted` để tự `EditorApplication.Exit` | Đã chạy 1 lần, asset đã có ở `Assets/TextMesh Pro/` — không cần chạy lại trừ khi asset đó bị xoá |

## Input

| File | Nội dung |
| --- | --- |
| `Assets/Input/GameControls.inputactions` | Action map "Gameplay": `Move` (Vector2, WASD composite), `Zoom` (Axis, scroll), `Interact` (Button, E — `InteractionDetector`), `ToggleInventory` (Button, I/Tab — `InventoryPanel`), `ToggleMap` (Button, phím M — `WorldMapPanel`, S8), `Close` (Button, phím Esc — `ContainerPanel`/`WorldMapPanel`/`BuildPanel`/`ShelterPanel`, 2026-07-24), `ToggleBuild` (Button, phím B — `BuildPanel`, S11), **`ToggleShelter`** (Button, phím N — `ShelterPanel`, S12), **`ToggleEvents`** (Button, phím V — `EventPanel`, S14) |

## Data định nghĩa game — CONTENT THẬT + SCENE ĐÃ NỐI (S5+S6+S7+S8+S9)

`Assets/StreamingAssets/Definitions/` — `manifest.json` (`definition_version: 0.9.0`) + `README.md` + content thật:
- `items_p1.json` — 5 item: `item_water_bottle` (0.8kg/1.0L st4, `use_effects.thirst:-40`), `item_canned_food` (0.4/0.5 st8, `use_effects.hunger:-50`), `item_battery` (0.15/0.2 st10), `item_toolbox` (8/12 st1), `item_water_container_20l` (18/30 st1, `two_hand_carry:true`).
- `items_p2.json` (S8, mới) — 6 item trang bị: `item_jacket` (body, `wet_multiplier:0.3`), `item_boots` (feet, `exposure_block_level:1`+`exposure_medium_multiplier:0.5`), `item_gloves` (hands, `handles_contaminated:1`), `item_rope` (tool, `current_reduction:1`), `item_dry_bag` (back, `backpack_capacity_kg:10`+`backpack_capacity_liters:18`), `item_medkit` (tag "medical", `use_effects.health:50`). **Chưa gắn vào searchpoint nào** — chưa có cách nhặt trong gameplay thật, chỉ debug Add Item.
- `locations_p1.json` — `location_shelter` (scene `20_MainShelter`, `is_shelter:true` S7), `location_convenience_store` (scene `41_Location_ConvenienceStore`), nối bởi 1 route.
- `routes_p1.json` — `route_shelter_store` 25 phút game, `base_elevation_level` mặc định 0 (S8, field mới).
- `searchpoints_p1.json` — 6 điểm gắn với `location_convenience_store` (2 kệ nước, 2 kệ khô guaranteed, quầy/kho có entry theo `chance` — xem hybrid loot S6-follow-up 2026-07-24).
- `balance.json` — khớp default `BalanceConfig` + section `condition` (S7, +3 field shelter recovery S9) + section `hazard` (S9, viết tường minh — giá trị khớp default C# đã có từ S8, hành vi không đổi).
- `phases_p2.json` — 4 `DisasterPhaseDefinition`: dry@0, first_rain@30, black_rain@80, route_closure@140 (game-minute) — S8: nay thật sự lái Flood/Current level qua `HazardRules`.
- `shelterzones_p3.json` (S10, mới) — 8 `ShelterZoneDefinition` đúng main-shelter-design.md §6-14: shelter_entrance(2 slot)/central_hall(0)/ground_storage(2)/utility_area(2)/water_processing(2)/workshop(1)/upper_living(3)/roof(1) = 13 build slot total.
- `modules_p3.json` (S11, mới) — 5 `ModuleDefinition`: module_barrier (entrance, 4 wood+2 scrap/60'), module_pump (utility_area, pump_part+2 scrap/45', tag "pump"), module_elevated_storage (ground_storage/upper_living, 3 wood/40'), module_purifier (water_processing, purifier_unit+filter/50'), module_battery_bank (utility_area, 2 battery+1 scrap/30').
- `items_p3_materials.json` (S11, mới) — 5 vật liệu xây dựng: item_wood/item_scrap/item_pump_part/item_purifier_unit/item_filter. Gắn tạm vào loot `searchpoint_back_room` (chỗ thật là gara S17).
- `events_p3.json` (S13; +S14) — 3 `EventDefinition`: event_drain_backflow (Phase BlackWater + State≥Damp, response "reinforce_seal"), event_storage_flood_risk (State≥Shallow, response "secure_storage"), event_pump_jam (cần Pump active + roll 1%/long-tick, response "clear_pump", soft_deadline 30' + hard_deadline 60' — quá hạn: pump kẹt vĩnh viễn vì không còn resolve được).

✅ Từ S6: content này KHÔNG CÒN chỉ là dữ liệu — mỗi search point/route/location đều có GameObject/scene thật dùng nó (xác nhận qua headless smoke: boot → SceneFlowController load đúng `20_MainShelter`).

## Render / Project settings đã cấu hình (S1)

- URP asset: `Assets/Settings/LastHope_URP.asset` (+ `LastHope_Renderer.asset`), gán vào `GraphicsSettings` + toàn bộ Quality level.
- Color space: Linear.
- Packages đã thêm: `com.unity.render-pipelines.universal@17.5.0`, `com.unity.inputsystem@1.20.0` (⚠ 1.11.2/1.12.0 lỗi compile với Unity 6000.5.4f1 — không hạ version), `com.unity.nuget.newtonsoft-json@3.2.1`, `com.unity.modules.physics@1.0.0`.
- Build Settings scenes (thứ tự): `00_Boot` → `10_GamePersistent` → `90_TestSystems` → `Shelters/20_MainShelter` → `Locations/41_Location_ConvenienceStore`.

---

## Việc CHƯA làm (để tránh giả định nhầm khi đọc code)

- **M1 (S2-S4) PASS**, **S5/S6 PASS → Gate P1 PASS (2026-07-24)**; **S7/S8 PASS**; **S9 PASS → Gate P2 PASS về mặt kỹ thuật (2026-07-24)**: 127/127 EditMode test, build Windows 0 lỗi, headless smoke xác nhận boot sạch với toàn bộ hệ thống P2. P2 (Condition/Disaster Phase/Hazard/Equipment/Travel risk/Shelter recovery/Scenario A-D) coi như xong về kỹ thuật — xem `docs/backlog/BACKLOG.md` mục Gate P2 để biết phần cần user tự chơi thử xác nhận.
- `DebugOverlay` (F1) và `DebugPanel` (F2) là 2 file khác nhau — F1 luôn hiện (FPS/vị trí), F2 toggle riêng (World Time/Save/Condition cheat/Equipment/Rest at Shelter/Phase jump/Add Item/Travel cheat/state dump).
- `StartTaskCommand`/`CancelTaskCommand` vẫn chỉ validate + ghi flag/log, KHÔNG có effect thật — đó là việc của Shelter Task (S10+, xem `docs/plans/2026-07-24-p3-p4-completion-plan.md`). `BeginTravelCommand` ĐÃ đầy đủ từ S6+S8.
- `StartSleepCommand` fast-forward clock nhưng KHÔNG kiểm tra event/interrupt (chờ S12 viết lại — Sleep Simulation nằm trong plan P3, không phải P2). Ngủ hiện tại vẫn tích luỹ hunger/thirst/fatigue bình thường qua ShortTick/LongTick trong lúc FastForward — không có gì đặc biệt khác `RestAtShelterCommand.Rest` ngoài thời lượng.
- `black_water` exposure có 3 nguồn thật: crossing route ngập (`BeginTravelCommand` S8), cầm đồ Contaminated không gloves (`TransferItemCommand` S8), và giảm qua `RestAtShelterCommand.TreatExposure` (S9) — không còn dormant.
- `items_p2.json` (jacket/boots/gloves/rope/dry_bag/medkit) **vẫn chưa gắn vào searchpoint/loot table nào** — chưa có cách người chơi NHẶT được trong gameplay thật, chỉ test qua debug Add Item + unit test. Đặt loot thật là việc content của P3+.
- **Không có `ProtectionSpec.cs` riêng** dù plan gốc S8 có nhắc tên file này — dùng `ItemDefinition.Protection:Dict<string,float>` (có sẵn từ S7) đủ biểu diễn mọi hiệu ứng cần, tránh 1 lớp cấu trúc song song.
- `RouteState` (cache hiển thị, HazardSystem ghi mỗi long-tick) và giá trị THẬT dùng để validate travel (`BeginTravelCommand` tự tính qua `HazardRules` tại đúng thời điểm) là 2 con đường tách biệt có chủ đích — đừng sửa `BeginTravelCommand` để đọc `RouteState` tưởng là tối ưu, nó sẽ tái tạo lại vấn đề "stale 9 phút".
- `RouteState.Electrical`/`Modifiers` (S8) là field rỗng chưa ai dùng — đặt trước cho Power System/Shelter Event (P3+).
- **Playtest thật đã bắt đầu (2026-07-24, sau S9)** — user báo I/Tab và E "không hoạt động", lộ ra 3 bug lifecycle MonoBehaviour tồn tại từ S6 (ContainerPanel/InventoryPanel) + S8 (WorldMapPanel, copy nhầm pattern) — đã sửa, xem mục "Bug phát hiện qua playtest thật" trong `docs/backlog/BACKLOG.md`. **Bài học quan trọng: `Assets/Scenes/*.unity` phải được regenerate qua `SceneSetup.BuildAll()` MỖI KHI sửa `SceneSetup.cs`** — scene đã lệch code từ S6 tới S9 (3 sprint) mà không ai phát hiện vì test tự động không chạm tới scene thật, chỉ build+headless smoke (không kiểm tra input/UI thực sự hoạt động). Từ giờ: sau khi sửa `SceneSetup.cs`, PHẢI chạy lại `-executeMethod LastHope.EditorTools.SceneSetup.BuildAll` trước khi build/test, không chỉ chạy `BuildScript.BuildWindowsDevelopment`.
- User vẫn cần tự chơi thử theo đúng kịch bản Gate P2 (30-45 phút) sau khi các fix trên: đổi route vì flood, đổi loadout trang bị, hiểu return window trên World Map (M), xử lý exposure tại shelter (Rest/Treat/Dry Off qua F2), xem ConditionHud hiển thị đúng góc trái trên màn hình.
- `InventoryPanel` chưa có nút Drop dù owner `location_dropped:` đã hoạt động được — chỉ thiếu nút UI.
- `ContainerPanel`/`InventoryPanel` chưa gọi `TelemetryLogger.Log()` cho `item_left_behind`/`inventory_open_time` — API có sẵn, chỉ chưa được UI gọi tới.
- Chưa có interactable trong world để trigger `RestAtShelterCommand`/`StartSleepCommand` — cả 2 chỉ gọi được qua Debug Panel/test tới lúc này, giống tiền lệ Sleep từ M1. Interactable thật (vd tương tác với giường/khu nghỉ) là việc UI/content của P3+.
- **P3 (Shelter Loop, S10-S13) XONG → Gate P3 PASS về mặt kỹ thuật (2026-07-24)**. P4 (Vertical Slice, S14-S18) CHƯA VIẾT GÌ. Plan chi tiết ở `docs/plans/2026-07-24-p3-p4-completion-plan.md`.
- **S13 (Event Framework) mới xong — những gì CHƯA có**: Event Discovery (Undiscovered/Discovered) hoàn toàn chưa làm — mọi event trigger là auto-Active ngay, người chơi luôn "biết" ngay lập tức, không có cơ chế "event đang diễn ra mà chưa phát hiện" như thiết kế (chờ S14). Deadline (soft/hard) được LƯU trên `ActiveEventState.DeadlineMinute` nhưng KHÔNG có gì tự động expire khi qua deadline — event cứ Active mãi tới khi resolve tay, không có hậu quả "bỏ mặc" (chờ S14 `RegisterThreshold`). Event Budget (giới hạn Critical/Major/Standard đồng thời, event-system-design.md §21) chưa enforce — có thể có bao nhiêu event Active cùng lúc cũng được. Event Chain, Persistent Consequence, off-scene guarantee (system không đọc vị trí player) đều chưa làm — tất cả là S14. `ResolveEventCommand` dùng switch cứng theo response id (không phải resolution_rules data-driven) — chỉ work với đúng 3 response đã biết, thêm event mới phải sửa code, không chỉ sửa JSON.
- **S12 (Power/Water/Sleep) — vẫn đúng từ S13**: Purifier/Pump bị Power gate thật; chưa có UI cảnh báo mất điện chủ động (chỉ hiện khi mở ShelterPanel); Filter hết chưa có cách sửa/thay (chờ NPC Repair S16). `TaskKind.Active`+`RequiredWorker` trong TaskSystem VẪN chưa sprint nào thực sự dùng.
- **S11 (Build System) — vẫn đúng từ S12**: Elevated Storage module xây được nhưng chưa có hiệu ứng dung lượng riêng.
- **S10 (Water Intrusion) — S13 đã lấp nốt gap**: Drain Backflow event giờ set cờ `ShelterEventFlags.DrainBackflowActive` thật (trước S13 luôn `false`). `DrainCoreView` vẫn chỉ đọc, không tương tác Clean/Seal trực tiếp (Pump Jam response "clear_pump" giải qua ResolveEventCommand/DebugPanel, không phải tương tác world với Drain Core).
- Ramp lên Upper Floor trong `20_MainShelter` là blockout thô (box xoay góc tự tính) — chưa xác nhận bằng mắt CharacterController leo được mượt, chỉ xác nhận qua headless smoke (boot + scene switch không exception).
- **BuildPanel (B, S11) + ShelterPanel (N, S12) + Events section trong DebugPanel (F2, S13) chưa test tay** — các nút bấm chưa được xác nhận hoạt động trong game thật, chỉ qua EditMode test. Vật liệu xây dựng hiện chỉ có ở `searchpoint_back_room` (convenience store).
- **Sleep interrupt (S12) chưa test tay** (nhưng ĐÃ có cách thử): F2 Debug Panel có nút "Sleep" — chưa ai tự tay xác nhận ngủ qua đêm có thức sớm khi shelter ngập lên Deep+ hay không trong game thật, chỉ qua EditMode test.
- **Pump Jam event (S13) khó tự test tay** — roll chỉ 1%/long-tick nên hiếm khi tự nhiên xảy ra trong 1 phiên chơi ngắn; dùng nút "Force-trigger" trong DebugPanel Events section để test không cần chờ RNG.

## Ghi chú kỹ thuật quan trọng (tránh dò lại code để hiểu "tại sao")

- RNG dùng xorshift64* tự viết (không dùng `System.Random`) vì cần expose state để serialize và tiếp tục sequence bit-exact sau load — xem `RngStream.cs`.
- `DefinitionLoader` không ném exception cho lỗi data (chỉ throw nếu JSON không đọc được, và exception đó cũng bị bắt + gom vào `Errors`). Gọi `Load()` luôn trả về `DefinitionLoadResult`, không bao giờ throw ra ngoài với input hợp lệ về mặt cấu trúc file. `balance.json` là NGOẠI LỆ — thiếu/lỗi parse fallback default, không tính vào `Errors`.
- Naming JSON trên đĩa là **snake_case**, nhưng C# property là PascalCase — đừng thêm `[JsonProperty]` thủ công, `SnakeCaseNamingStrategy` tự convert.
- **TextMeshPro cần "TMP Essential Resources" import trước khi dùng** — nếu thiếu, mọi `TextMeshProUGUI.Awake()` throw NullReferenceException lúc runtime (không phải lúc compile!). Đã import (`Assets/TextMesh Pro/`), đừng xoá folder đó.
- `PlayerAvatarSync` là **ngoại lệ có chủ đích** của nguyên tắc "mọi thay đổi state qua Command" — vị trí liên tục (continuous data) không phải rule, ghi thẳng vào `PlayerState.Position*` mỗi frame. Đừng dùng pattern này cho state rời rạc khác.
- SceneSetup giờ tạo UI 100% bằng code (không prefab) — `InventoryPanel`/`InteractionPrompt` tự dựng hierarchy trong `Awake()`/`BuildLayout()`, không phụ thuộc asset `.prefab` nào.
