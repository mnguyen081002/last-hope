using UnityEngine;

namespace LastHope.Presentation.World
{
    /// <summary>Marker used by SceneFlowController to place the player when no saved position
    /// matches the current scene's location (e.g. first-time arrival).</summary>
    public sealed class PlayerSpawnPoint : MonoBehaviour
    {
    }
}
